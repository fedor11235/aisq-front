export type Coffee = {
  id: number;
  size: string;
  count: number;
};

export type CoffeeNew = {
  size: string;
  count: number;
};

export type CoffeeCountUpdate = {
  id: number;
  count: number;
};
